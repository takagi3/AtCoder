use proconio::input;
use proconio::marker::Chars;

fn main() {
    input! {
        r: usize,
        c: usize,
        sy: u32,
        sx: u32,
        gy: u32,
        gx: u32,
        map: [Chars; r],
    }
}
